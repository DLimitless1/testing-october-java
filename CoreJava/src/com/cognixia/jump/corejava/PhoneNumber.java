package com.cognixia.jump.corejava;

import java.util.regex.Pattern;

import com.sun.org.apache.xerces.internal.impl.xs.identity.Selector.Matcher;

public class PhoneNumber {

	public static void main(String[] args) {
//Problem 2:
	// Attributes
		String str1 = "Hello";
		String str2 = "HELLO";
		String str3 = "WORLd";
		String str4 = "WORld";
		String str5 = "hi";
	//Constructor
		if(str1 == str1){}
		if(str2 == str1);
		if(str3 == str1 && str3 == str2);
		if(str4 == str1 && str4 == str2 && str4 == str3);
		if(str5 == str1 && str5 == str2 && str5 == str3 && str5 == str4);
		
		for (char x = uniqueWord.compareTo; x > 0; x++) {
			str1.equals(str1);uniqueWord[x].getName());
		
	str1.equals(str1);
	str2.equals(str1);
	str3.equals(str1);
	str4.equals(str1);
	str5.equals(str1);
	
	System.out.println(" " + str.compareTo());
	System.out.println("How many unique words are there?");
		}
//Problem 3:
	public static boolean validatePhoneNumber(String phone, String invalidator{
	
	String regex = "^\(\d{3}\)\s?\d{3}-\d{4}$";
	Pattern pattern = Pattern.compile(regex);
	String str = "3104849040";
	Matcher matcher = pattern.matcher(str);
	System.out.println("String Matches: " + matcher.matches());
		}
	}
}
